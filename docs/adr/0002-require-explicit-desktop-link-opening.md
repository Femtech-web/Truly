# Require explicit desktop link opening

Truly carries workspace links from the phone to a paired Mac, but never opens a creator- or AI-supplied destination silently. The phone shows the destination during handoff and the Mac requires a visible **Open** action, with only HTTPS and local-development HTTP URLs accepted, because a seamless remote open would also create an easy phishing and unattended-device trigger.
