# Path schema

This legacy package directory will hold the versioned creator Path format while internal `skill` identifiers are migrated. A Path version contains outcomes, compatibility, creator identity, access rules and ordered steps. Each step can define an outcome, guidance, one reviewed workspace link, supporting resources, a challenge and a completion rubric.

Learner-owned Tasks are not creator manifests and do not belong in this package.
